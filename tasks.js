// Tell the user to enter two numbers then show the multiply the numbers and show the result
var ftHeight = prompt ("Enter the height in (ft)");
// convert to number
ftHeight = parseFloat("fHeight");
var inHeight = ftHeight * 12;
//convert to cm
var cmHeight = inHeight * 2.54;
result =inHeight * 2.54;
alert('The height in cm is', + cmHeight.toFixed(2) );

